name: totem_workspace_viewer
description: Production Flutter viewer for the TotemWorkspace architecture and development graph.
publish_to: none
version: 0.2.0+2

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/graph-data.json
